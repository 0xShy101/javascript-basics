let name = 'TheShy101';
console.log(name);

// Variable name rules:
// Cannot be a reserved keyword
// Should be meaningful
// Cannot start with a number (1name)
// Cannot contain a space or hyphen (-)

// Variables are case-sensitive EXAMPLE

let firstName = 'Shy';
let FirstName = 'Alex';

// these are completely different Variables

// ----------------------------------------

// You can have more than one Variable in a single line using (,) EXAMPLE

let City = 'NYC', Country;

// City is in this example is defined, We can know this by seeing that it has a (=)
// Country is undefind because it does not contain a (=) in it
